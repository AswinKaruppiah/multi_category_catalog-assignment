const Topbar = () => {
  return (
    <header className="py-5 bg-white border-b border-gray-200 flex items-center px-4 gap-3 shrink-0 z-50">
      <span className="text-xs text-gray-400 tracking-wide">
        Multi-category
      </span>
    </header>
  );
};

export default Topbar;
