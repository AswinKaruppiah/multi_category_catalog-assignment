import { CarIcon, PhoneIcon, ComputerIcon, BikeIcon } from "@/utils/icons";

export const CATEGORY_ICONS = {
  Cars: CarIcon,
  Phones: PhoneIcon,
  Computers: ComputerIcon,
  Bikes: BikeIcon,
};
